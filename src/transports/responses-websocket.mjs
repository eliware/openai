// SDK-specific transport access is isolated here so the public adapters do not
// depend on the SDK's resource tree.
import { ResponsesWS } from 'openai/resources/responses/ws';

export { ResponsesWS };
