function isObject(value) { return value !== null && typeof value === 'object'; }
export function normalizeEvent(message, raw = message) { if (!isObject(message)) return message; const event = message.type === 'message' && isObject(message.message) ? message.message : message; const nestedError = isObject(event.error) ? event.error : undefined; const response = isObject(event.response) ? event.response : undefined; return { ...event, raw, responseId: event.response_id ?? event.responseId ?? response?.id, requestId: event.request_id ?? event.requestId ?? event._request_id ?? nestedError?.request_id ?? nestedError?.requestId, ...(event.error === undefined ? {} : { error: event.error }) }; }
