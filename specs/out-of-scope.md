# Out of scope

The package does not execute tools, persist tool state, or provide hosted-tool
or modality-specific billing. It does not support Azure-specific construction,
the removed `responses-ws` alias, implicit WebSocket reconnect behavior, or
browser-native WebSocket constructors.
